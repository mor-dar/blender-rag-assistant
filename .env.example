# Blender Bot Configuration

# === API Keys MUST DEFINE ONE ===
# Groq API Key
GROQ_API_KEY=your_groq_api_key_here

# OpenAI API Key
OPENAI_API_KEY=your_openai_api_key_here

# === Model Configuration ===
# Groq Models
GROQ_MODEL=llama3-8b-8192
GROQ_TEMPERATURE=0.7
GROQ_MAX_TOKENS=2048

# OpenAI Models
OPENAI_MODEL=gpt-5-mini
OPENAI_TEMPERATURE=0.7
OPENAI_MAX_TOKENS=2048

# === Memory Configuration ===
# Memory type: "none" (default), "window", or "summary"
MEMORY_TYPE=none

# Window memory settings (when MEMORY_TYPE=window)
# Number of recent messages to keep in conversation history
MEMORY_WINDOW_SIZE=6

# Summary memory settings (when MEMORY_TYPE=summary)  
# Maximum tokens before conversation gets summarized
MEMORY_MAX_TOKEN_LIMIT=1000
